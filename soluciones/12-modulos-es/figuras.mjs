// Soluciones módulo 12 — módulo de figuras (ejercicios 1, 3 y 4).
export function areaCuadrado(lado) {
  return lado * lado;
}

export function areaRectangulo(base, altura) {
  return base * altura;
}
